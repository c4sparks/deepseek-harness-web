# 03 · 界面交互与用户手册

> 阶段：设计/交互/用户 · 维护者：插件维护者

## 布局

侧边栏聊天视图 `dsh.launcher` 顶部为标题栏，下方消息区(flex:1) + 输入区(composer) + 底部 statsbar。

标题栏有两种实现（同一常量 `TITLEBAR_MODE` 切换，当前默认 `nativeTitle`；**不自动判宿主**——nativeTitle 即原生、selfDrawn 即自绘）【v0.1.5 · dsh 0.1.2-rc.1】：
- 原生标题栏：由宿主在 webview 视图头渲染（`menus.view/title`）。宿主渲染得出原生头即生效（默认）；渲染不出（如 sidex 等）则切 `'selfDrawn'`。
- 自绘标题栏：页面内 `#titlebar`，跨宿主通用；渲染不出原生头的宿主用它。

自绘标题栏按钮自左向右：`> 工作区名`（指示，悬停显全称）、spacer、📁（工作区选择）、＋（新会话）、浏览器打开/刷新（仅 DSH 面板打开时）、本地打开、移到编辑器、⋯（消费记录）。👁 关闭侧栏预留但默认隐藏。详见 docs/details/03。

## 视觉与组件规范
| 条目 | 规范 |
|---|---|
| 色彩 | 全用 `var(--vscode-*)`，禁硬编码；标题栏背景取 `--vscode-sideBar-background`（与原生 view/title 同底色），边框 `--vscode-panel-border`；头像 `--vscode-badge-background`（中性灰，无渐变） |
| 图标 | 标题栏用内嵌 Codicon（`codicon.css` base64 字体）；输入区权限/模型/模式用内联 SVG currentColor |
| 标题栏按钮 | 命中区 24×24，Codicon 16px；hover 灰底 `rgba(128,128,128,0.22)`；隐藏项不占位 |
| 标题栏间距 | `#titlebar` flex `gap:2px`；spacer `flex:1 1 auto`；📁/⋯ 等图标容器 `flex:none`（防窄栏被压缩溢出叠邻居） |
| 工作区名指示 | `> 名`：名称宽裕时全显，过长省略号 + 悬停 title 显全称；上限 64% |
| 工作区下拉 | 页内浮层，`position:fixed` 铺满侧栏宽（窄侧栏也可见）；顶部搜索框 + 当前工作区行 + 工作区树（可展开会话）＋ 底部「新建工作区…」 |
| 会话行 | 当前会话：✓ + 「当前」；运行中：sync + 「运行中」；其余：history + 「恢复」；长名省略 + 悬停全称 |
| 面板态联动 | 默认只显示「本地打开 ⬇」；点它打开 DSH 面板后切为「浏览器打开 🌐 + 刷新 ⟳」（原生按钮显隐由 `dshPanelOpen/dshViewMode` 上下文同理控制） |
| 输入栏布局 | `#inputbar` flex 垂直居中，图标按钮 22×22 `flex:none`，组间 gap 4px；发送/停止 24×24 圆形居右 |
| 输入区收窄 | `.sel-group` `flex:0 1 auto; min-width:22px`（保图标不被压小）；文字 label 无死上限，宽时全显、窄时省略号缩到 0；图标固定等距、不叠发送按钮 |
| 输入区边框 | `#inputbox` 常显 1px 边框（主题变量优先）；聚焦换 `--vscode-focusBorder`；`#composer` 无顶部分割线 |
| 图标按钮 hover | ＋/权限/模型/模式 hover 圆角灰底（`rgba(128,128,128,0.28)`），active 用 0.35 |
| 弹层配色 | 权限/模型/模式选项 hover 与选中统一用主题列表色（`--vscode-list-hoverBackground` / `--vscode-list-activeSelectionBackground`），不用按钮蓝，浅色模式清晰【v0.1.5】 |
| 通用弹窗 | `showDialog()`(modal.ts) 统一确认/提示；danger 确认带 ack 勾选 |
| 发送/停止 | 发送=内联 SVG 细线上箭头，停止=内联 SVG 方块；CSS 用 stop 类切换，stop 时按钮红底 |
| 行首 / 命令菜单 | 输入框行首打 "/" 弹菜单，按官方展示：分组「指令」(commands/list)+「技能」(skills/list)，无插件本地组；分派同官方：permission 走可搜索选择弹窗(SearchPicker，选即关)、无参命令立即执行、带参命令/技能插入 "/name " 且进入参数阶段后菜单不再弹、普通 Enter 即执行；模型图标入口用独立 ModelPicker(分组+推理等级，点选即关)；命令结果一律回显在对话区(成功正常/失败红点红字)，plan 常驻 chip【0.1.6】 |
| 消息渲染 | 用户右/dsh 左；markdown 过 DOMPurify；图片内联 max-height 240px；思考折叠「已思考 · N 步 · N 工具」。悬停动作：提问行仅复制；回答行 复制/重新生成 |
| 对话区滚动 | 智能跟随（无缝对话流）：贴底即自动跟随到底；用户向上拖（哪怕 1px，按方向判定）立即解除跟随、不抢阅读位置；回到底部（<24px）恢复；主动发送/重新生成/恢复会话无条件滚到底（scrollPend）【0.1.6 续】 |
| 输入框 @ 引用 | 行首/空白(含换行)后打 `@`（或 `@"`）弹候选，按查询串实时拉（`fileReferences/list` + `sessionReferenceResolver/candidates`）。候选分两大类别：**「文件与文件夹」**（目录=fileReferences `kind:'directory'`、文件=`file`，图标 codicon `folder-opened`/`file` 区分；目录行右侧 codicon `chevron-right` 可下钻）与**「对话」**（codicon `comment-discussion`）。展示只显短名 + 命中段高亮；层级：`@` 空=顶层、`@x/`=该目录直接子级、纯名字才深层模糊；下钻时弹窗顶部显示面包屑 `工作区 › 目录…`（细线分隔后再列列表）。选中（行身点击/Enter）= 加蓝色引用贴片，贴片**叠在输入框首行行首**（textarea 全宽、仅首行 text-indent 让位，回车/换行回最左；光标在最左时 Backspace 删贴片）；目录右侧 chevron/Tab=钻取（保留 `@目录/` 继续列子项）。发送时 store 把贴片 token（`@rel/path`/空白 `@"…"`/目录带 `/`/会话 `@[label](dsh-session:…)`）作引用行注入 prompt（正文不出 @；dsh 宿主 pre-step 解析），**气泡内引用以蓝色高亮 chip 内联展示（仅真换行才换行）**。模块 `core/trigger/at.ts`（与 `/` 同 trigger 框架）【2026-09-08】 |
| 消息时钟 | 每条消息动作行左侧时刻，对齐官方日期感知（同日 HH:mm / 今年更早 M月D日 HH:mm / 跨年 Y年M月D日 HH:mm）；来源 = user/assistant 消息 epoch time【0.1.6】 |
| 回复用量/用时 | 每条已完成回答的内容下方**默认常显**两个图标按钮（📊 用量、⏱ 用时，图标旁带数值），点击弹出官方「本轮用量」「本轮用时和速度」弹窗【v0.1.6】（默认向上；消息位于列表最上方、上方放不下时自动翻到消息下方；宽度随内容自适应，提供方/模型等内容短则收窄、长则保持一行封顶 320px）：用量（本轮用量）= 累计投影差分（未缓存输入+缓存读+输出=本轮用量；另列提供方/模型、缓存命中、其中推理）；用时（本轮用时和速度）= 本轮总用时/TPS/TTFT（TTFT、TPS 取累计投影 ttftMs/ttftSteps、decodeMs/decodeTokens 差分；总用时取服务端 turn 事件墙钟）。历史恢复行同样显示（用量=该条 assistant/message 自带 usage；用时由快照事件时间推算）。实现：`webview/chat/components/TurnStats.ts` + `src/dsh/cumulative.ts` |
| 已知限制（历史计时） | 历史恢复的 ⏱ 弹窗里 TTFT/TPS **依赖快照保留 `step/start` 与 `text-delta` 时间**；若上游把历史压缩成只剩 `assistant/message`，TTFT/TPS 无数据可算 → 显示 “—”，**不伪造**（用量/总用时仍可显示） |
| statsbar | 底部仅一条官方累计 statsbar（与回复用量相互独立） |

## 消息桥接（webview ↔ 扩展）
| 方向 | type |
|---|---|
| webview→ext | ready/chatSend/cancel/approvalResponse/questionResponse/questionCancel/chatSelectPermission/chatSelectModel/chatSelectMode/pickFile/copy |
| ext→webview | chatChunk/chatReasoning/chatActivity/chatApproval/chatQuestion/questionClosed/chatDone/chatHistory/chatInfo/filePicked/draft/clear |

握手：webview 发 `ready` 后扩展才推 `chatInfo`（避免切回丢状态）。自绘标题栏另有专属通道 `titleAction/wsDropdown/selfInfoReq`（见 docs/details/03 §标题栏通道）。

## 交互反馈
发消息打字机出现；思考先占位再折叠摘要；需授权/提问显示聊天内卡片，经本地应答后 agent 继续，■ 可停止兜底；danger 权限走确认；进行中按钮变 ■；错误给明确人读文案。非法输入/图片就地限制，不静默。

## 用户手册
1. 安装：扩展面板搜本插件（以 `package.json` 的 displayName 为准）或 `code --install-extension <vsix 路径>`；需本机已装并运行 dsh。
2. 打开：点 Activity Bar 的 DSH 图标。
3. 聊天：底部输入/贴图/拖文件/`＋`；`↑` 发送。
4. 模型：🤖 选模型再选推理等级。
5. 权限：🛡 直列预设；danger 需确认。
6. 工作区：点标题栏 📁 或 `> 工作区名` 附近选择；工作区下拉里可搜、展开会话、新建；当前会话标「当前」。新建会话必须归属工作区（规则见第 14 条）。
7. 右键代码：处理/提问/@。
8. 完整界面：标题栏「在本地打开/在浏览器打开」。
9. 消费记录：`⋯ → 查看消费记录`（今/昨/周/月分组）。
10. 审批/提问：聊天内直接确认；若卡片未出现，用「在本地打开/在浏览器打开」到 dsh 官方页处理。换工作区=独立会话；升级 dsh 后打不开则重开视图或按 04 核对。
11. 会话模式：输入区层叠图标选择；仅空白/新会话可切换，已有会话只读显示模式。
12. 查看用量/用时：每条回答内容下方默认显示 📊 用量 / ⏱ 用时（带数值），点击弹出官方「本轮用量」/「本轮用时和速度」详情（默认向上，消息在列表最上方放不下时自动翻到下方；与 dsh 官方口径一致）。提问行悬停只有 复制，回答行悬停才有 复制/重新生成。
13. 行首 `/`：输入框行首输入 `/` 弹指令/技能菜单（按官方展示：分组「指令」「技能」，无插件本地组；指令如 权限/compact/goal/plan，技能在下方列出）；选指令执行、选技能插入后发送；命令结果/错误会显示在对话区（成功正常色、失败红点红字），plan 模式时输入区出现 `plan` 小徽标（点击退出）；有会话目标（goal 投影）时输入区常驻 `🎯` goal chip（悬停见全文，chip 内可暂停/继续/清除）。`/export` 会保存会话日志 ZIP 到本地。

14. 会话归属（防「未分组」）：新建会话必须先落在一个工作区——有当前工作区（或能自动取默认：dsh 里 `updatedAt` 最新 / 当前打开的文件夹）直接在其下新建；都没有时点 ＋ 会提示「请先选择工作区」并打开工作区面板，选/建工作区后才开会话，不会自动造出「未分组」会话。在 dsh 官方网页（标题栏「在本地打开/在浏览器打开」）里发起的会话由 dsh 核心按会话 cwd 归组，与侧栏聊天的归属规则相互独立。【v0.1.7(dev) · dsh 0.1.2-rc.1】

> 提示：原生标题栏按钮常驻依赖用户设置 `workbench.view.alwaysShowHeaderActions: true`；未开启时按悬停/激活规则显示。自绘标题栏（`TITLEBAR_MODE='selfDrawn'`）无此限制。

## 更新记录

### 0.1.0（2026-08-30）
- 新增：
  - 初始版本

### 0.1.2（2026-09-03）
- 新增：
  - 界面交互与用户手册文档
- 修改：
  - 安装步骤改用占位符，不写具体扩展标识
  - 审批/提问：改为 `$events` 流聊天内本地应答，网页仅作未命中兜底
  - 消息桥接表补 questionCancel/questionClosed
  - 输入区规范补常显边框、无分割线、悬停灰底与 SVG 发送/停止图标

### 0.1.3（2026-09-05）
- 修改：
  - 标题栏改回原生 view/title；移除页内自绘标题栏（后被 0.1.5 自绘双实现取代）
  - 标题栏按钮常驻依赖 alwaysShowHeaderActions
  - 历史会话恢复与新会话投递链路稳定

### 0.1.4（2026-09-05）
- 新增：
  - 会话模式选择：输入区层叠图标 + 置顶弹窗；空白会话可切换，已有会话只读

### 0.1.5（2026-09-06）
- 修改（标题栏改为原生/自绘双实现，默认原生）：
  - 标题栏改为两种实现、常量 `TITLEBAR_MODE` 切换：原生 view/title 与页内自绘 `#titlebar`（默认原生）
  - 自绘标题栏按钮：`> 工作区名`（悬停显全称）、📁 工作区选择、＋ 新会话、本地打开/浏览器打开/刷新（按 DSH 面板开态联动）、移到编辑器、⋯ 消费记录；👁 关闭侧栏隐藏不占位
  - 移到编辑器图标用 `open-preview` 镜像对齐原生（放大镜方向）
  - 工作区下拉改为 `position:fixed` 铺满侧栏宽（窄栏可见）；会话当前态标 ✓「当前」；空会话隐藏只保留当前/运行中
  - 输入区收窄规则：图标固定等距、文字可缩到无，发送/停止不叠图标
  - 弹层 hover/选中统一主题列表色，去按钮蓝（浅色清晰）
  - 标题栏去掉宿主自动判定：移除 appName 白名单 / hostRendersNativeTitlebar / resolveTitlebarMode，`TITLEBAR_MODE` 直接决定原生/自绘；默认取 nativeTitle（渲染不出原生头的宿主再切 selfDrawn）

### 0.1.6（2026-09-06）
- 新增：
  - 回复用量/用时：回答内容下方默认常显 📊/⏱ 两图标（带数值），点击向上弹官方「本轮用量」「本轮用时和速度」弹窗（用量=本轮用量/提供方·模型/缓存命中/未缓存输入/缓存读/输出·其中推理；用时=本轮总用时/TPS/TTFT）；提问行仅复制、回答行悬停才有 复制/重新生成；历史恢复行同样可显示用量与用时
  - 回复用量改用“累计投影差分”口径（与官方一致）：TTFT=ΔttftMs/ΔttftSteps，TPS=Δoutput/(ΔdecodeMs/1000)，本轮用量=Δ输入+Δ缓存读+Δ输出；总用时取服务端 turn 事件墙钟
  - 模型目录 failures：模型弹窗顶部“N 组模型加载失败”提示
  - 回合终止原因(end-note)：aborted/interrupted/max-tokens/blocked/error 不再静默当正常完成
  - 原始帧日志开关（实验取证）：`env DSH_RAWLOG=1/full`
- 修改：
  - 消息时间改用 API `assistant/message` 事件 time；正文以整条 `assistant/message` 为准覆盖流式拼接
  - 本轮统计只取 `assistant/message.usage`（整轮累计，不自算步数）；消费记录存 API 时刻、本轮差分用量
  - 审批卡 toolName 直显（reason 只放真实原因）；question 选项 description 正文化；底部统计条与回复统计补 cacheWrite
  - user 行重生成保留原图；错误以 end-note 呈现不再塞进正文；停止/打断回合补记已消耗用量
  - 抽取专有模块：差分 `src/dsh/cumulative.ts`、用量/用时组件 `webview/chat/components/TurnStats.ts`（后续改口径/样式只动这两处 + `stream.ts` 的 wallSec）

### 0.1.6-rc.2（2026-09-07）
- 修改/新增：
  - 消息时钟对齐官方日期感知：同日 HH:mm；今年更早 M月D日 HH:mm；跨年 Y年M月D日 HH:mm（实时回答与历史恢复一致）
  - 「本轮用量」「本轮用时和速度」弹窗宽度随内容自适应（短内容收窄、长内容保持一行封顶 320px）；列表最上方消息弹窗自动翻到下方展开，避免被顶部截断
  - 行首 `/` 命令/技能菜单：数据源 = dsh 命令目录 `commands/list` + 会话技能 `skills/list`；按官方展示分组「指令 / 技能」（无插件本地组）
  - 输入触发架构收敛：通用 `core/trigger/useTrigger.ts` + 每触发一文件（`trigger/slash.ts`），Composer 只注册 defs；宿主侧拆独立 `src/chatInputService.ts`；dsh 层命令/技能拉取留 `src/dsh/rpc.ts`
  - 斜杠入口分派对齐官方：permission（host 命令）选后走可搜索选择弹窗 `components/SearchPicker.ts`（选即关）；无参命令立即执行；带参命令/技能插入 `/name ` 且「/指令 参数…」阶段菜单不再弹、普通 Enter 即执行
  - 模型不属 host 命令（官方由客户端弹窗贡献）：斜杠菜单不提供 `/model`，仅在输入区模型按钮入口用独立组件 `components/ModelPicker.ts`（分组 + 推理等级，点选即关）
  - 切模型推理等级对齐官方 `selectionOf`：同 model 保留现等级，换 model 用其 `reasoning.defaultEffort`（`defaultEffort` 已透传至 webview）
  - 命令结果一律进对话区（成功正常色/失败红点红字）；permission/model 由 `/` 发起也追加结果行（markSlashPick）
  - plan 状态 chip：读投影 `plan{active,pending}`，放图标行下方独立 `#chipRow`；悬停提示、点击 `/plan off`（pending 置灰禁用、带 ` ✕`）；状态命令执行后宿主等 300ms 再刷新投影
  - 输入区空高 56→68px；composer 底边/`#chipRow`/底部统计(tok)行间距收紧
  - 详细逐命令对照见 `docs/details/upgrade/07` §11
  - 【2026-09-08 续】`/` 命令链完善：
    - 目录兜底：会话建立(chatInfo)后预取命令/技能目录；目录未到即发 `/xxx` 时先挂起该行、取回目录再裁决（命中命令→执行，未命中→普通消息，避免把 `/compact` 当普通文本发 agent）
    - `/export` 真实下载：宿主直连 `GET /api/session.export`（rc1 流式 ZIP）→ 保存对话框写文件，结果以路径回对话区；运行中 dsh 无该路由时回退为仅回显命令文本
    - goal 常驻 chip：读投影 `goal{goal:{objective,phase}}`（null=无/undefined=能力缺失），与 plan chip 同排 `#chipRow`；active/paused/blocked 显示、complete 隐藏；chip 内 ⏸/▶/✕ 分别执行 `/goal pause|resume|clear`
    - plan chip 修复：状态命令(/plan /goal)执行后宿主对投影做短轮询 —— dsh 空闲即 committed、有 open turn 时把切换 queued 到回合边界才落定，原单次 300ms 刷新会停在旧值（chip 不消失/不生效）
  - 【2026-09-08 续】对话区智能跟随滚动（“无缝对话流”）：
    - 语义：贴底即自动跟随到底；用户向上拖（哪怕 1px，按方向判定）立即解除跟随、不抢阅读位置；回到底部（<24px）恢复；主动发送/重新生成/恢复会话无条件滚到底（scrollPend）
    - 实现：`webview/chat/core/store/chat.ts` 增 `scrollPend`（主动触底计数，send/suggestion/regenerate/renderHistory 各 +1）；`webview/chat/components/app.ts` MessageList 用 `stickRef` + `onScroll`（按上次 `scrollTop` 方向判定上拖）决定是否写 `scrollTop = scrollHeight`，替代原「距底 <48px 才滚」的近似（大段一次性内容/异步图片高度变化时贴底跟随易脱底）
  - 【2026-09-08 续】输入框 @ 引用（与 `/` 同 trigger 框架的独立模块 `core/trigger/at.ts`）：
    - 触发：行首/空白(含换行)后 `@` 或 `@"…`，光标在 token 尾（对齐官方 grammar）；命中即 `requestAtList(query)`（单飞行+最新查询 wins+同查询去抖）
    - 数据源（rc.1）：文件/目录 `fileReferences/list` args `{agentId,query}` → `{path,kind}`；会话 `sessionReferenceResolver/candidates` args `{agentId,query}` → 含 `mention`；宿主 `src/dsh/rpc.ts` + `chatInputService.listAtRefs()`，协议增 `atListReq`/`atCatalog`
    - 展示与分组：两大类别——「文件与文件夹」（目录 `kind:directory`/文件 `kind:file`，图标 codicon `folder-opened`/`file` 区分，目录行右侧 codicon `chevron-right`(20px) 可点）与「对话」（codicon `comment-discussion`）；行=短名 + 命中段高亮（`searchText` 用全路径/会话名+id 过滤、展示仍短名）；底色/边框/投影与权限/模型弹窗同一 `.popup` 底
    - 层级：只列当前一层——`@` 空=顶层、`@x/`=该目录直接子级、纯名字才深层模糊
    - 选中与钻取：行身点击/Enter = 加**蓝色引用贴片**（不清 token 进正文）；目录右侧 chevron 或 Tab = 钻取（保留 `@目录/`、菜单保持并继续列子项，可逐级）；下钻时弹窗顶部显示面包屑 `工作区 › 目录…`（`.trigger-path`，下衬细线再列列表）
    - 贴片：蓝色小片，**叠在输入框首行行首**（textarea 全宽，effect 按 #refRow 实测宽度设 `text-indent` 只缩进首行 → 回车/换行回最左）；`✕` 或 光标在最左时 Backspace 删贴片，图标同 codicon
    - 发送：store 把贴片 token（`@rel/path`/空白 `@"…"`/目录带 `/`、会话 `@[label](dsh-session:<b64url>)`）作引用行注入 prompt（发给 dsh），**气泡展示文本不含 @token**，改由用户消息行携带 ref 元数据、以蓝色高亮 chip `.msg-ref-chip` 内联渲染（flex-wrap，仅真换行才换行）
  - 【2026-09-08 续】@ 引用 UI/UX 视觉专节（大部分与 `/`、权限/模型弹窗统一，个别属 @ 特殊）：
    - 菜单浮层（共用）：底色 = 浮层色板链 `editorSuggestWidget → editorWidget → menu`（回退 editor-background）；边框 `editorSuggestWidget-border → dropdown-border → panel-border`；`box-shadow: inset 0 1px 0 rgba(255,255,255,.05) + 0 6px 22px rgba(0,0,0,.45)`；行悬停 = `list-hoverBackground`、选中 = `list-activeSelection`；分组标题 `.optgroup-label` 11px/透明度。**无下划线/划线装饰**（若加分割一律用 1px `panel-border`）
    - 命中高亮 `.trigger-hl`：字色 `textLink-foreground`(回退 charts-blue)+ `font-weight:700`；行图标 `.trigger-ico` codicon 13px
    - @ 特殊：行图标 codicon `folder-opened`/`file`/`comment-discussion`；目录行右侧钻取按钮 `.trigger-drill` = codicon `chevron-right`，字号 20px、灰、悬停变亮；下钻顶部路径条 `.trigger-path` = 11px 灰 + 底部 1px `panel-border` 细线分隔
    - 输入框贴片 `.ref-chip`：底色 `rgba(117,190,255,.14)`、字/边 `charts-blue`、胶囊圆角(10px/20px 高/11px 字)；absolute 叠首行(left=8px、不占高、max-width 62%)；**top 动态** = `paddingTop + 实际行高/2 − 贴片高/2`(实测 textarea 行高字体自适应，CSS 兜底 6px)使贴片中心对准首行文字中心；textarea 以测量宽度设 `text-indent` 只缩进首行 → 后续行回到最左；图标 codicon 12px
    - 贴片区间距细则(`#refRow`/`#input`)：**上** top=6px（距输入框内上边）；**左** left=8px（贴片靠左；textarea `padding-left` 仍 14px → 贴片右端与文字起始间约留 6px）；**右/宽** `max-width:62%`、多 chip 横向滚动(滚动条隐藏)；**内部** chip 之间 gap=4px、chip 内 padding `0 7px`、图标/文字间距由 flex gap；textarea `padding: 10px 14px 4px` 保持原值、`min-height` 无贴片 68px；贴片与首行**垂直中心对齐**（贴片中心 ≈ 首行文字中心）；无贴片时 `#refRow` 不占位(absolute)，text-indent 清零
    - 气泡内引用 chip `.msg-ref-chip`：同蓝胶囊（圆 9px/字号 11/底 rgba(117,190,255,.14)/边 charts-blue），`flex-wrap` 内联、仅真换行换行；图标同 codicon 12px
  - 【2026-09-08 续】浮层可读性：`.popup` 底色由 `editor-background`（与对话区同色）改为浮层色板链 `editorSuggestWidget → editorWidget → menu` + 更亮边框 + 顶部 1px 提亮 + 更深的投影（`/`、`@`、权限/模型/模式弹窗通用），避免与聊天背景融为一体看不清
  - 【2026-09-08 续】消息列宽差异化：`.msg .col` 通用 min-width:0；**发送者(user) col max-width 78% + row-reverse 贴右侧（卡片随内容自适应）**、**回复(assistant) col max-width 100% 满宽**，均随对话区宽度自适应（原先两侧同 86%）

### 0.1.6（2026-09-08）
- 新增/修改（0.1.6 发布记录，细节见上 0.1.6-rc.2 段内【2026-09-08 续】各条）：
  - 输入框行首 `/` 调用「指令 / 技能」（无本地组）；`@` 引用「文件与文件夹 / 对话」（目录下钻 + 引用贴片 + 气泡内引用 chip）；`/export` 保存会话日志 ZIP
  - plan/goal 常驻 chip、技能目录 `skills/list` 修正、滚动智能跟随、消息列宽差异化、弹窗浮层底色/可读性

### next（2026-09-10）
- 新增/修改：
  - ＋新会话 / 发消息：会话必须归属工作区。有当前或可自动默认工作区直接新建；无工作区提示「请先选择工作区」并弹工作区面板，不再自动建「未分组」会话（用户手册第 14 条）
  - 明确侧栏聊天与会话归属规则、与 dsh 官方网页 iframe（dsh 核心按 cwd 归组）相互独立
