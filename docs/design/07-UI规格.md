# 07 · UI 规格（尺寸/间距/圆角/字号/动效）

> 本表为**当前实现值**，来源 `webview/chat/styles/chat.css`、`chain.css`、`state.css`（改样式后请同步本表）。
> 单位 px；变量优先 `--vscode-*`（深/浅主题自适应），列出的色值仅兜底。

## 1. 全局 / 布局
| 项 | 值 |
|---|---|
| 基准字号 | 13px |
| body 溢出 | hidden（整个 webview 不滚页，列表内滚） |
| #app 布局 | flex 纵向，`flex:1 1 auto; min-height:0` |
| 消息列表 #messages | 上下 12 / 左右 14 padding；行间距 gap 16；`flex:1` 纵向 overflow-y auto |
| 列表滚动条 | 细(自带) |

## 2. 标题栏 / 欢迎
| 项 | 值 |
|---|---|
| 页内 #titlebar | padding 上2 左右8(右8 下2/左6 见代码) 实质 `2px 8px 2px 6px`；底部 1px border；font 11 |
| 标题/动作按钮 | 命中区 24×24；codicon 16；radius 4；hover 背景 rgba(128,128,128,.22) |
| 欢迎标题 | font 20/600 |
| 欢迎描述 | font 13、次级色 |
| 建议按钮 | radius 16；padding 上6 左右12；font 12 |

## 3. 消息行（对话区）
| 项 | 值 |
|---|---|
| .msg 布局 | flex 行；gap 10；align-items flex-start |
| .msg .col | flex 列；gap 4；min-width 0 |
| user .col | max-width 78%；align-self flex-end |
| user 气泡 .body | 背景=inputBackground；radius 10；padding 上8 左右10；pre-wrap |
| 头像(预留) | 28×28 圆 |
| 消息动作条 .msg-meta | font 11；gap 6；opacity 0→hover/最新 1；过渡 0.15s；**动作条（复制/重新生成/用量/用时）仅回答结束后(done)渲染**，回答过程中不出现（按用户要求；上游为 data-actions-reveal hover，插件定稿后现） |
| .msg-actions 按钮 | padding 上2 左右5；radius 4；font 12（codicon 13） |
| 用量/用时钮 | 高18~20；padding 0 5~6；radius 4 |
| 缓存命中率口径 | `formatCacheHitPercent(cacheRead, totalTokens−outputTokens, 1)`（`core/format.ts cacheHitPercent`）：**1 位小数**(26.4%)、整数去尾 `.0`、`100` 特判；分母=uncached+cacheRead+cacheWrite |
| status-badge | font 10；padding 0 7；radius 8；行高1.6 |
| end-note | font 11；error 色 |

## 4. markdown 正文（assistant）
| 项 | 值 |
|---|---|
| 正文基 | font 13；line-height 1.6；color foreground；overflow-wrap anywhere |
| 段间距 p | 下 0.6em |
| h1/h2/h3/h4 | 17/15/14/13（600）；h1 下边框；标题段距 上1.1em 下0.5em |
| 列表 | padding-left 22；li 上下 0.15em |
| 引用 blockquote | 左边框 3 + 圆角右 6；淡背景 |
| 行内 code | padding 0 4；radius 4；font 12；浅底+1px hairline |
| 代码块 pre | padding 10 12；radius 8；font 12；行高1.55；横向滚动 |
| 表格 | th/td padding 上4 左右9；1px hairline；font 12.5 |
| 流式光标 .is-typing::after | 宽2 高13；蓝；1s step-end 闪烁 |

## 5. 回合过程（chain.css）

### 5.0 链内纵向节奏（本插件自定，**不参照上游**）
| 相邻 | 净距 | 来源 |
|---|---|---|
| 过程行 ↔ 过程行（思考/工具/上下文注入） | 4 | 行 `margin: 2px 0` 相加（`.chain-detail` 不设 gap，避免与 margin 叠加） |
| 折叠头 ↔ 首个过程行 | 4 | `.chain-detail` margin-top 2 + 首行 margin-top 2 |
| 可点击行 ↔ 其展开体（思考全文框 / 工具 ioCard） | 2 | 展开体上 margin 2；**展开体内部子元素不得设更大的纵向 margin**，否则折叠取大值会顶掉这 2px |
| 末行展开体 ↔ 正文 | 12 | 行 margin 2 + `.chain` margin-bottom 6 + `.msg .col` gap 4 |
| 回合过程块 ↔ 正文（未展开） | 10 | `.chain` margin-bottom 6 + `.msg .col` gap 4 |
| 上一条消息 ↔ 本 assistant 行 | 16 | `#messages` gap |

### 5.1 折叠头 / 容器
| 项 | 值 |
|---|---|
| .chain | font 11.5；margin 上2 下6 |
| .chain-summary | inline-flex gap6；padding 上2 左右6；radius 6；font 11.5；hover 透明底 |
| .chain-summary-ico / -chev | codicon 12 |
| .chain-summary-text（折叠头文案） | nowrap；超长 ellipsis；min-width 0 |
| .chain-detail | 上 margin 2；**不设 gap**（行距由各行 margin 相加，见 §5.0） |

### 5.2 思考/工具 Disclosure 行
| 项 | 值 |
|---|---|
| .chain-disclosure | 上下 margin 2（与上下文注入行 `.ctx` 的 2 一致：链内三种过程行等距） |
| .chain-detail 行距 | 容器**不设 gap**，行距 = 相邻两行 margin 相加 = **4**（2 + 2）；折叠头到首行 = `margin-top` 2 + 2 = 4 |
| .chain-row-head | flex 行 gap 6；padding 上2 左右3；radius 5；width 100%；font 11.5；nowrap+keep-all；hover 透明底 |
| .chain-chev / .chain-kind-ico / .chain-tool-ico | 12px，flex none |
| .chain-row-title / .chain-tool-title | 600；nowrap；超过省略 |
| .chain-sep（标题与摘要之间的分隔圆点） | 2×2；margin `0 3px`；radius 1；次级色 |
| 失败行的收起摘要 | **真失败（isError，宿主 `item.status === 'error'`）→ 结果文本首行**，`.chain-row-preview.is-error` 着错误色（对齐上游 `failureLine ?? description ?? summary`）。**非零退出/被信号终止的 shell 不算这类**（`isError: false`，退出状态是结果数据）→ 摘要**仍是模型写的 `description`**；它的失败由行首红点 + 卡内红 Pill 表达。裸错误码（`.chain-tool-err`）**不显示**。**提问行不走这条**（上游 `AskQuestionRow` 不传 `errorSummary`）。见 `06` §10.1 |
| 文件类工具的收起摘要 | = **相对工作区根**的路径（**跟上游** `abbreviateHomePath(relativizeToCwd(路径, 工作区根), home)`；Windows 上 home 缩写是空操作）——工作区内的文件显相对路径，**工作区外的显示全路径，不缩成文件名**。卡片内部路径另行按上游（读卡相对化 / 差异卡 verbatim / 搜索卡原样）。见 `06` §10.2 |
| .chain-tool-dot（**行首**状态点，上游 StateDot） | 10×10；`margin 0 1px`；**两层结构**：`::before` 整圆 halo（currentColor + opacity .1）+ `::after` inset 20% 实心内核（10px 里 6px 实心）；`is-error` 走 errorForeground、`is-warning` 走 charts-yellow。**只在 error/stopped 时取代变体图标**；正常/进行中仍显 `.chain-tool-ico`。行尾无状态标记 |
| .chain-row-state（行状态读屏文字） | 视觉隐藏（1×1 + clip-path inset(50%)）——状态点与掠光对 AT 不可见，靠它播报 运行中/失败/已停止 |
| .chain-row-head[data-state='running']::after（② 掠光带） | **固定宽 300px**；定位锚 `position relative` + `overflow hidden`；渐变 `transparent → 主题底 60% (55% 处) → transparent`（`color-mix`，把字形与图标洗向背景）；`chain-row-sweep 2.6s ease-out infinite`：`left: -300px → 100%`（**末段 90–100% 停在行外一拍**）；`prefers-reduced-motion` 下关闭。**取代原先行尾的转圈角标** |

> 掠光带的宽度适配要点：**带子宽度固定 300px、位移用 `left` 而非 `transform`**。起点 `-300px` 是绝对像素（整条带子在行外，与行宽无关），终点 `100%` 相对**行宽**（带子左缘贴到行右缘）。因此侧栏（窄）与编辑器页（宽）都能完整扫过，且**光带视觉宽度恒定**——用 `width: 40%` 或 `translateX(350%)`（相对自身宽的百分比）会让窄栏光带缩成一条、宽栏拉成一大片。
| 正文预览 .chain-row-preview | font 11；line-height 1.5；flex 1 1 auto、min-width 0；**收起态单行** `nowrap+ellipsis`——宽不足才省略、无固定字数上限；全文靠展开 |
| .user-atts（用户消息的附件行） | flex 列；align-items flex-end（随用户气泡右对齐）；gap 6；底部 margin 6（与气泡分开） |
| .user-file-chip（气泡外的文件 chip） | inline-flex；gap 5；max-width 240；padding `3px 8px`；radius 6；font 12；`EXT 大小` 次级色 11 |
| .turn-status（左下角状态行） | 行内 flex、gap 8、高 26、margin `4px 0 2px`；文字 `600 12px`（`.turn-status-text` 现覆盖为 10px）、图标 11px、时钟 13px + `tabular-nums` |
| .chain-file-link（可点文件路径：收起行摘要 + 图片卡标签） | 沿用所在容器的字号与省略规则；`border:none; background:none; padding:0; margin:0; font:inherit; cursor:pointer`；**虚线常显** `underline dotted` + `text-decoration-thickness: 1px` + `text-underline-offset: 3px`，虚线色=次级色；hover/focus-visible 文字与虚线一起转 foreground/currentColor；收起行里点击不触发展开（组件 stopPropagation） |
| 展开体 .chain-disclosure-body | 上下 margin 2（与上方可点击行的区分间距；整卡 `margin: 0 0 0 4px` 缩进 4）|

### 5.3 思考正文框 / 工具参数
| 项 | 值 |
|---|---|
| .chain-reason | margin 上下 2、左右 2（上 2 = 与「思考」标题行的区分间距）；padding 上2 左右8；radius 5；font 11.5；行高1.55；淡底 |
| .chain-reason.is-peek（收起预览态） | max-height 84；overflow hidden；底部 40px 渐隐遮罩（`::after` 透明→editor 底色）；cursor pointer |
| .chain-reason.is-live | max-height 160；纵向内滚（自动贴底） |
| .chain-reason.is-open | max-height none |
| .chain-arg-k | 次级色；右 margin 8 |
| .chain-arg-v | foreground；pre-wrap |

通用工具卡的「输入 / 输出」分区 ioCard（通用路径与提问卡兜底都用它）【v0.1.7 · dsh 0.1.2-rc.1；v0.1.9 起提问卡兜底也走 ioCard，原先只服务该兜底的两条直排样式（`.chain-tool-out`/`.chain-tool-json`）随之删除】：
| 项 | 值 |
|---|---|
| .chain-io-card | flex 列；**纵向 margin 恒为 0**（交给外层 `.chain-disclosure-body` 的 2，子元素更大会在折叠中取大值顶掉它）；左 margin 4（缩进）；0.5px hairline 边框；radius 12；bg=textCodeBlock；font 11(等宽) |
| .chain-io-section | grid `max-content 1fr`；column-gap 14；align-items baseline；padding `12px 16px`；max-height 150 内滚 |
| .chain-io-label | 标签「输入」/「输出」；sticky top 0 + align-self start（分区内滚时标签常驻）；次级色；nowrap |
| .chain-io-text | min-width 0；`pre-wrap` + `break-word`；foreground 色；`.is-error` 时用 error 色（失败调用的输出） |
| .chain-io-divider | 两分区之间的 hairline：高 0.5px；full 宽（在两段 padding 之间，不在网格内）；仅「输入」与「输出」都存在时渲染 |

### 5.3b 图片卡 / 附件图廊（v0.1.9 新增）
| 项 | 值 |
|---|---|
| .img-card | flex 列；gap 8；margin `4px 0 4px 4px`；padding `12px 14px`；radius 12；bg=textCodeBlock；font 13 |
| .img-label | 路径标签；等宽 font 12；foreground 色；`word-break: break-all` |
| .img-meta（信封文本） | font 12；次级色；`pre-wrap` + `overflow-wrap: anywhere` |
| .att-gallery（附件层图廊） | flex 换行；gap 8；align-items flex-start |
| .att-item | 无边框按钮；radius 8；`cursor: zoom-in`；`line-height: 0`（图片不留基线缝）；focus-visible 描边 focusBorder + offset 2 |
| .att-item.is-loading / .is-error | 虚线 0.5px 边框；`min-width 120`、`min-height 40`；padding `10px 12px`；error 态 `cursor: pointer`（点=重试） |
| .att-note | font 12；次级色 |
| 单图尺寸（内联 style） | 长边 ≤240 且**不放大**超原始像素；显示比例 clamp 到 `[0.25, 4]`，越界 `object-fit: cover`（极宽左对齐 / 极瘦顶对齐） |
| 多图尺寸（内联 style） | 固定 64×64 瓦片；`object-fit: cover` 居中 |
| .att-lightbox（看全尺寸） | fixed 覆盖；backdrop `rgba(0,0,0,0.72)`；padding 24；图 `max-width 92vw` / `max-height 88vh`；radius 8 + 阴影 |

### 5.4 Terminal 卡（上游 TerminalBlock.module.css 复刻，bash/pwsh/shell）
| 项 | 值 |
|---|---|
| .term | radius 12；gutter 左 pad 30；margin `4px 0 4px 4px`；0.5px hairline 边框；bg=textCodeBlock；font 12(等宽)；overflow hidden |
| .term-head（命令横幅） | flex；gap 12；margin-left -30(拉平)、padding `9px 14px 9px 30px`；max-height 150 内滚；定稿下 0.5px 底分隔 |
| .term-prompt-line | 每命令一行；line-height 22；gap 8 |
| .term-state（运行态文字标签） | 视觉隐藏（1×1 + clip-path inset(50%)）——状态点与配色对 AT 不可见，靠它播报 运行中/失败/已完成 |
| .term-dot（状态点） | 绝对定位 left -22、垂直居中；10×10；**两层结构同上游 StateDot**：`::before` 整圆 halo（currentColor + opacity .1）+ `::after` inset 20% 实心内核（10px 里 6px 实心）。色走 `color`：ongoing 蓝 / done 绿 / error 红 / warning 琥珀 |
| .term-cwd | 提示标签（`~`/末段/`$`）；tertiary 色；flex none |
| .term-cmd | **white-space pre**（保留对齐）；超长 ellipsis；primary 色 |
| .term-status（退出码/信号 Pill） | sticky top；高 22；error 红 |
| .term-copy 按钮 | sticky top；font 12；tertiary 色；无 border；复制原始输出(剥 marker) |
| .term-output | padding `12px 14px 12px 0`；**max-height 224 + 纵向内滚**（输出可能上千行）；**横向也内滚**（`overflow-x auto`）；不折叠行 |
| .term-line | min-height 22；**white-space pre 不折行**（终端输出表格化/对齐，折行会撞散列对齐；超宽交给输出体横滚） |
| 终端输出的转义剥离 | 剥 **CSI + OSC + 其它非 CSI 转义 + 无显示含义的 C0 控制字节**（覆盖面照上游 `ui-primitives/ansi.ts`；保留 `\t \n \r \b ESC`）。**漏剥 OSC 会让行变宽**——pwsh 提示符写的 OSC 133（`ESC ] 133;D;<code> BEL`）会以 `]133;D;0` 残渣留在不折行的输出里，凭空撑出横向滚动条。本插件不做着色、不重放光标，故只剥不画 |
| .term-empty（无输出） | padding `12px 14px 12px 0`；tertiary 色 |

### 5.4b 差异卡 / 读文件卡 / 搜索卡（上游 DiffBlock / ReadBlock / SearchBlock 复刻）【v0.1.7】
三张卡**长内容按上游 8 行上限折叠中间**：首尾各半（`headLines = ceil(8/2)`、`tailLines = 8 - headLines`）+ 「… 其余 n 行」按钮（上限常量 `CHAT_BLOCK_MAX_LINES = 8`）。**终端卡不在此列**——它的输出是「224px 上限 + 纵向内滚、不折叠行」（见 §5.4），滚动条已足够查看，不再叠折叠。
**三卡的行一律 `white-space: pre` + body 横向滚动（不折行）**：上游对代码/路径/匹配行都用 pre——折行会撞散缩进与列对齐。

| 项 | 值 |
|---|---|
| 三卡容器（`.diff-block` / `.read-block` / `.search-block`） | margin `4px 0 4px 4px`；0.5px hairline 边框；radius 12；bg=textCodeBlock；font 12(等宽)；行高 22 |
| 差异卡 .diff-copy | 绝对定位 `top 8px right 12px`；无边框；次级色，hover 转前景色 |
| 差异卡 .diff-body | padding `12px 14px` |
| .diff-line | min-height 22；**white-space pre 不折行**，超宽靠 `.diff-body` 横滚 |
| 差异卡 .diff-line.is-path | 600；padding-right 56（给浮层复制按钮留位） |
| .is-del / .is-add | `::before` 画 `- ` / `+ `，且**符号与整行正文同时着色**（error / success）——符号+颜色双通道表意 |
| 差异卡 .diff-footer | padding `0 14px 12px`；次级色；内容 `└ +N -M · K 个文件` |
| 读文件卡 .read-banner | flex；baseline；gap 8；padding `9px 14px`；**独立底色 + 顶部圆角 12**（上游 `markdown-code-block-banner`；VS Code 无对应 token，用 `rgba(128,128,128,0.06)` 中性叠层兜底） |
| .read-count / .read-lang | 次级色；**字段字号不一**——count 13px、lang 12px/18px 等宽；**行数注记只在 `lines.length < totalLines` 时出现** |
| 读文件卡 .read-copy | `margin-left auto`；无边框；次级色 |
| 读文件卡 .read-body | padding `12px 0`；横向滚动 |
| 读文件卡 .read-gutter | 宽 48；右对齐；padding-right 14；**user-select none**（行号不进复制） |
| 读文件卡 .read-content | min-width 0（内容列，随 gutter 排布） |
| 搜索卡 .search-header | flex；baseline；gap 8；padding `9px 14px`；**独立底色 + 顶部圆角 12**（同 read 横幅，中性叠层兜底） |
| .search-summary | 次级色 13px；单行 ellipsis（`N 处匹配 · M 个文件` / `N 个路径`） |
| 搜索卡 .search-copy | `margin-left auto`；无边框；次级色，hover 转前景色 |
| 搜索卡 .search-body | padding `8px 14px 12px 0` |
| 三卡的折叠按钮（`.diff-expand` / `.read-expand` / `.search-expand`） | 整行可点；无边框透明底；次级色，hover 转前景色；左缩进与各自内容列对齐（read 48 = gutter；search 14；diff 0）。文案 `… 其余 {n} 行` / 展开后 `收起` |
| 搜索卡 .search-file-head | matches 分支的**文件头按钮**：flex；gap 6；padding `0 14px`；min-height 22；hover 透明底（点它收起/展开该文件的匹配） |
| 搜索卡 .search-file-path | `flex 1 1 auto`；600；单行 ellipsis |
| 搜索卡 .search-file-count | `flex none`；次级色（该文件的匹配数） |
| .search-line | min-height 22；padding-left 14；**pre 不折行**（body 横滚） |
| 搜索卡 .search-line-number | 次级色（`行号: ` 前缀） |
| 搜索卡 .search-empty | padding `12px 14px`；次级色（无结果） |
| 搜索卡 .search-recovery | padding `0 14px 12px`；折行；次级色（截断时的兜底定位符） |

### 5.5 上下文注入/召回行（上游 ContextInjectionRow.module.css / ContextBody.module.css 复刻）
| 项 | 值 |
|---|---|
| .ctx-head | flex 行；gap 6；padding 上2 左右3；radius 5；nowrap+keep-all；hover 透明底 |
| .ctx-chev / .ctx-ico | codicon 12；flex none；次级色 |
| .ctx-title | 600；nowrap 省略 |
| .ctx-sep（分隔圆点） | 2×2；margin 0 8；radius 1 |
| .ctx-source（来源标签） | flex none；单行省略；tertiary 色；font 11.5 |
| .ctx-summary（notice 单行说明） | flex 1 1 auto；单行省略；tertiary 色；font 11.5 |
| .ctx-body（展开体） | width `calc(100% - 22px)`；max-height 141；margin 上4 左22；padding `10px 16px 12px 12px`；radius 8；bg=textCodeBlock；font 11（等宽） |
| body 内文本 .ctx-text | `white-space: pre-wrap; overflow-wrap: anywhere` |

上下文注入 body 的**各 form 细分**（同一个 body 按 form 换内容；骨架见上，下面是每种 form 的块）：

| form | 选择器 | 值 |
|---|---|---|
| 通用字段表 | `.ctx-fields` / `.ctx-field` / `.ctx-field-key` / `.ctx-field-value` | fields 列 gap 2、上 margin 8、上 padding 8、顶部 0.5px hairline；field 行 gap 8；key `flex none; min-width 96`＋次级色；value `flex 1 1 auto`＋次级偏暗色、overflow-wrap anywhere |
| instructions（已协调文件） | `.ctx-files` / `.ctx-file` / `.ctx-file-path` / `.ctx-file-action` | files：flex wrap、gap `4px 12px`、下 margin 8、无 list 符号；file 行 gap 6、baseline 对齐；path 用前景色；action 用次级色 |
| catalog（目录变更） | `.ctx-notice` / `.ctx-entries` / `.ctx-entry` / `.ctx-entry-name` / `.ctx-entry-description` | notice：下 margin 6＋次级色；entries 列 gap 4；entry 行 gap 8；name `flex none`＋前景色；description `flex 1 1 auto`＋单行 ellipsis＋次级偏暗 |
| snapshot（子系统快照） | `.ctx-sections` / `.ctx-section` / `.ctx-section-name` / `.ctx-section-text` | sections 列 gap 8；section 列 gap 2；name 次级色；text 前景色＋pre-wrap |
| recall（跨会话召回） | `.ctx-recalls` / `.ctx-recall` / `.ctx-recall-label` / `.ctx-recall-counts` | recalls 列 gap 2、下 margin 8、无 list 符号；recall 行 gap 8；label 前景色＋overflow-wrap anywhere；counts `flex none`＋次级色 |
| 未知/畸形兜底 | `.ctx-unknown` / `.ctx-unknown-label` / `.ctx-unknown-json` | unknown 上下 margin 6；label 次级色、font 11、下 margin 2；json 原始等宽 pre-wrap（不解析、不求美化） |
| 源字段 .ctx-fields | 顶部 0.5px hairline 分隔；gap 2；fieldKey 左 96 固定、caption 色；fieldValue 自适应、tertiary 色 |
| 各 form 列表 | files(flex wrap gap 4/12)、entries/recalls(纵向 gap 4/2)、sections(纵向 gap 8)；entry-name/section-name 次级、description/counts caption 色 |

### 5.6 Web 卡（上游 WebBlock.module.css 复刻；web_fetch/web_search）
| 项 | 值 |
|---|---|
| .web | margin `4px 0 4px 4px`；padding `12px 14px`；radius 12；bg=textCodeBlock；font 13；color foreground |
| .web-fetch | 纵向 flex；gap 6；布局=**URL 链接行(globe 图标) → `.web-status`(下一行)** |
| .web-fetch-row | 状态行整行：flex、baseline 对齐、gap 12、允许换行、min-width 0 |
| .web-status | font 13/600；次级色；位于 URL 链接**下一行**（用户指定：状态在链接下；上游 URL 上、状态下） |
| .web-link-plain（非 http(s) 的 URL 原样文本） | 前景色，不做成锚点 |
| .web-link | 13px/500；line-height 19；link 色；等宽；break-all；hover 虚线下划线；链接前 **globe 浏览器图标** |
| .web-link-ico | 13px；vertical-align -2px；右 margin 5 |
| .web-truncated | 12px；tertiary 色；上 margin 8（fetch 卡内 0） |
| .web-answer | 下 margin 8；foreground；正文 markdown |
| .web-empty | 12px；次级色 |
| .web-sources | padding-left 2.5em；纵向 flex；gap 10；max-height 320；内滚 |
| .web-source-link | 14px/500；line-height 20；link 色；break-word；hover 虚线下划线 |
| .web-snippet | 13px/19；次级色；上 margin 2 |
| .web-published | 12px；tertiary 色；上 margin 2 |

### 5.7 提问卡（上游 AskQuestionCard.module.css 复刻；ask_user_question）
| 项 | 值 |
|---|---|
| .ask-card | flex 纵向；gap 16；max-height 360；margin `4px 0 4px 4px`；padding `16px 20px`；内滚；0.5px hairline 边框；radius 12；bg=editorWidget |
| .ask-item | 纵向；gap 2 |
| .ask-question / .ask-answer / .ask-verdict / .ask-q-unanswered | margin 0；13px；line-height 1.6；pre-wrap；问题/未答 tertiary、answer/verdict foreground |
| .ask-answer-line | block |
| .ask-skipped | tertiary 色 |
| .ask-question-list | padding-left 20；gap 8 |
| .ask-card | 提问记录：问题→答案列表 / 结论+问题列表（只读） |
| .q-dialog（waterfall 交互弹窗，composer 上方） | margin `0 14px 4px`；padding `12px 14px`；radius 8；bg=editorWidget；flex 列；gap 10 |
| .q-dialog-opt | padding `4px 10px`；radius 6；1px hairline；hover 透明底；selected 主题背景+focusBorder |
| .q-dialog-input | padding `6px 10px`；radius 6；1px hairline；bg=inputBackground |
| .q-dialog-submit / .cancel / .close | `回答`=button 底/前景（选全可点）；`取消`=透明+tertiary；`关闭`(✕)=margin-left auto |

对话行内的**待答交互**（.ask-pending 系；与上方 waterfall 弹窗 `.q-dialog` 是两处呈现，同一份数据）：

| 项 | 值 |
|---|---|
| .ask-pending | 与 `.ask-card` 同骨：flex 纵向 gap 8；margin `4px 0 4px 4px`；padding `16px 20px`；0.5px hairline；radius 12；bg=editorWidget |
| .ask-block | 单问块：flex 纵向 gap 6；min-width 0 |
| .ask-q-text | 问题原文：前景色 13px；pre-wrap |
| .ask-options | 选项容器：flex wrap；gap 6 |
| .ask-option | 胶囊：inline-flex gap 6；padding `4px 10px`；radius 6；1px hairline；透明底；font 12；hover 透明底(0.22)；.selected → focusBorder 边 + 主题按钮底 |
| .ask-opt-desc | 选项描述：次级偏暗色 |
| .ask-input | 自由输入：整宽；padding `6px 10px`；radius 6；1px hairline；bg=inputBackground；font 12；disabled → opacity 0.6 |
| .ask-bar | 底部按钮条：flex gap 8；上 margin 4 |
| .ask-submit / .ask-cancel | padding `4px 14px`；radius 6；无边框；font 12。submit=button 底/前景、disabled opacity 0.5；cancel=透明+次级色 |

## 6. 状态 / 过渡（state.css）
| 项 | 值 |
|---|---|
| .composer.is-busy | opacity 0.65；输入/发送 pointer-events none |
| .composer-busy | font 11；padding 上2 左右2 下5；gap 6；codicon 12 |
| busy 文案 | 正在加载会话… / 正在切换… |

## 7. 输入区 composer（chat.css）
| 项 | 值 |
|---|---|
| #composer | padding 上8 左右12 下4；editor 底 |
| #inputbox | radius 10；1px hairline；focus-within 主题 focusBorder |
| textarea #input | padding 上10 左右14 下4；font 13；min-height 68 / max-height 150 |
| #inputbar | padding 上2 左右10 下4 左2；gap 4 |
| 发送钮 #send | 24×24 圆；radius 50%；主题按钮底；stop 态 error 底 |
| 图标钮(权限/模型/模式) | 22×22；radius 4~6 |
| chips(plan/goal) | font 11；radius 10；gap 6 |

## 8. 弹层/通用（chat.css）
| 项 | 值 |
|---|---|
| .popup | radius 8；padding 6；list gap 1；浮层底+阴影 |
| #triggerPopup | width min(340,92vw) |
| .spicker 搜索框 | radius 5；font 12 |
| .spicker-list | 自身 `overflow-y: auto` 内滚（`.spicker` 与它同加 `flex:1 1 auto; min-height:0`）：定位当前项 / ↑↓ 移动时只滚列表，顶部搜索框钉住不被滚走 |
| #modePopup:focus | `outline: none`（`:focus-visible` 同）：容器只为承接键盘事件而聚焦(`tabIndex=-1`)，不显示 VS Code 默认焦点描边——选中态由行的 `.selected` 表达 |
| .modal-card | max-width 300；radius 10；padding 14；gap 12 |
| 按钮 .btn | radius 6；padding 上5 左右14；font 12 |

## 9. 动效
| 项 | 值 |
|---|---|
| 折叠头 chevron | transition transform 0.12s ease |
| msg-meta 显隐 | opacity 0.15s |
| 流式光标 | 1s step-end 无限 |
| LiveVerb 动词轮换 | 间隔 1.6s |
| 状态转圈 | codicon-modifier-spin（1.5s steps 30，内建） |

> 附：颜色类语义（fallback）charts-blue #4daafc / charts-green #89d185 / error #f14c4c / description #999~888。

## 更新记录

| 日期时间 | 插件版本 | 变更 | 维护者 |
|---|---|---|---|
| 2026-09-10 | 0.1.7 | 用词统一：「过程链」改称「回合过程」（§5 标题） | 插件维护者 |
| 2026-09-10 | 0.1.7 | §5.0 链内纵向节奏定案：过程行间净距 **4px**（`.chain-disclosure` 上下 margin 2、`.chain-detail` 不设 gap，行距只由相邻 margin 相加）；展开体与可点击行的区分间距 **2px**；ioCard 纵向 margin 0（避免折叠取大值顶掉父级 2px）。**间距本插件自定，不参照上游** | 插件维护者 |
| 2026-09-10 | 0.1.7 | §5.2 行状态标记对齐上游：删行尾角标，改**行首二选一**（异常显状态点、正常/进行中显变体图标）；running 由**行上掠光带**承担（不再是转圈）；补读屏文字 `.chain-row-state`。状态点按上游 `StateDot` 改**两层结构**（halo + 实心内核） | 插件维护者 |
| 2026-09-10 | 0.1.7 | §5.2 失败行收起摘要：**真失败（isError）**显结果文本首行并着错误色；**非零退出/被信号终止**仍显模型写的 `description`（退出状态是结果数据、不算失败）；裸错误码不显示。**提问行不走这条** | 插件维护者 |
| 2026-09-10 | 0.1.7 | §5.2 文件类工具的收起摘要 = **相对工作区根**的路径（跟上游；工作区外显全路径、不缩成文件名） | 插件维护者 |
| 2026-09-10 | 0.1.7 | §5.4 终端卡输出体定案：**`max-height 224` + 纵向/横向内滚、不折叠行**（`.term-line` 用 `white-space: pre` 不折行）。`.term-dot` 补 warning 态（与行首点同源，由 `TermCard.state` 决定） | 插件维护者 |
| 2026-09-10 | 0.1.7 | §5.4 新增「终端输出的转义剥离」：`stripAnsi` 补 OSC／非 CSI 转义／无显示含义控制字节（漏剥 OSC 会让 pwsh 的 OSC 133 残渣撑出横向滚动条） | 插件维护者 |
| 2026-09-10 | 0.1.7 | §5.4b 字段级样式修正：差异卡符号与正文同时着色；读卡/搜索卡横幅补独立底色与顶部圆角；三卡行 `white-space: pre` 不折行 + body 横滚；字段字号分化 13px/12px | 插件维护者 |
| 2026-09-10 | 0.1.7 | 补全像素登记并确立**本文档为像素值唯一归属**（`06` 里重复且已过期的折叠头样式表已删除）；删 5 个无引用的历史类 | 插件维护者 |
| 2026-09-10 | 0.1.7 | 修**相对化失效**（影响收起行摘要／读卡横幅／终端卡 cwd 标签）：`chatInfo.cwd` 改为原样返回作者写法（此前归一化后与工具给的路径前缀对不上，`relativizeToCwd` 静默失效）；webview 侧对盘符/UNC 路径加大小写与分隔符容忍 | 插件维护者 |
| 2026-09-11 | 0.1.8 | §8 补两条登记：`.spicker-list` 内滚（定位当前项不再把搜索框滚出视口）；`#modePopup:focus` 置 `outline: none`（容器只为承接键盘事件而聚焦） | 插件维护者 |
| 2026-09-12 | 0.1.9 | 修正 ioCard 段说明：提问卡兜底自 v0.1.9 起也走 ioCard，`.chain-tool-out`/`.chain-tool-json` 两条样式当前**无使用者**（原文写「仍供提问卡兜底」，已过期） | 插件维护者 |
| 2026-09-12 | 0.1.9 | §5.3b 新增图片卡 / 附件图廊的像素登记（新增 `.img-*` 与 `.att-*` 两类） | 插件维护者 |
| 2026-09-12 | 0.1.9 | 删除两条**无使用者**的样式（`.chain-tool-out`/`.chain-tool-json`：原只服务提问卡兜底，该兜底自 v0.1.9 起改走 ioCard）并同步删掉本节登记行 | 插件维护者 |
| 2026-09-12 | 0.1.9 | §5.2 补 `.chain-file-link` 一行（收起行摘要里的可点文件路径：样式规则与 hover 表现） | 插件维护者 |
| 2026-09-12 | 0.1.9 | §5.2 `.chain-file-link` 一行改准：虚线**常显**（原写 hover 才加）、适用范围含图片卡标签 | 插件维护者 |
| 2026-09-12 | 0.1.9 | 补 `.user-atts` 与 `.user-file-chip` 两项登记（用户消息附件行在外、正文气泡在下） | 插件维护者 |
| 2026-09-12 | 0.1.9 | 补登 `.turn-status` 一行（原先未登记）：文字 12px/600、图标 11px、时钟 13px；本次：文字由 12px 调为 10px | 插件维护者 |
