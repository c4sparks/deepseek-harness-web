# 08 · UI 像素级规格（尺寸/间距/圆角/字号/动效）

> 本表为**当前实现值**，来源 `webview/chat/styles/chat.css`、`chain.css`、`state.css`（改样式后请同步本表）。
> 单位 px；变量优先 `--vscode-*`（深/浅主题自适应），列出的色值仅兜底。

## 1. 全局 / 布局
| 项 | 值 | 出处 |
|---|---|---|
| 基准字号 | 13px | chat.css html/body |
| body 溢出 | hidden（整个 webview 不滚页，列表内滚） | — |
| #app 布局 | flex 纵向，`flex:1 1 auto; min-height:0` | — |
| 消息列表 #messages | 上下 12 / 左右 14 padding；行间距 gap 16；`flex:1` 纵向 overflow-y auto | — |
| 列表滚动条 | 细(自带) | — |

## 2. 标题栏 / 欢迎
| 项 | 值 |
|---|---|
| 页内 #titlebar | padding 上2 左右8(右8 下2/左6 见代码) 实质 `2px 8px 2px 6px`；底部 1px border；font 11 |
| 标题/动作按钮 | 命中区 24×24；codicon 16；radius 4；hover 背景 rgba(128,128,128,.22) |
| 欢迎标题 | font 20/600 |
| 欢迎描述 | font 13、次级色 |
| 建议按钮 | radius 16；padding 上6 左右12；font 12 |

## 3. 消息行（对话区）
| 项 | 值 |
|---|---|
| .msg 布局 | flex 行；gap 10；align-items flex-start |
| .msg .col | flex 列；gap 4；min-width 0 |
| user .col | max-width 78%；align-self flex-end |
| user 气泡 .body | 背景=inputBackground；radius 10；padding 上8 左右10；pre-wrap |
| 头像(预留) | 28×28 圆 |
| 消息动作条 .msg-meta | font 11；gap 6；opacity 0→hover/最新 1；过渡 0.15s；**动作条（复制/重新生成/用量/用时）仅回答结束后(done)渲染**，回答过程中不出现（按用户要求；官方为 data-actions-reveal hover，插件定稿后现） |
| .msg-actions 按钮 | padding 上2 左右5；radius 4；font 12（codicon 13） |
| 用量/用时钮 | 高18~20；padding 0 5~6；radius 4 |
| 缓存命中率口径 | `formatCacheHitPercent(cacheRead, totalTokens−outputTokens, 1)`（`core/format.ts cacheHitPercent`）：**1 位小数**(26.4%)、整数去尾 `.0`、`100` 特判；分母=uncached+cacheRead+cacheWrite |
| status-badge | font 10；padding 0 7；radius 8；行高1.6 |
| end-note | font 11；error 色 |

## 4. markdown 正文（assistant）
| 项 | 值 |
|---|---|
| 正文基 | font 13；line-height 1.6；color foreground；overflow-wrap anywhere |
| 段间距 p | 下 0.6em |
| h1/h2/h3/h4 | 17/15/14/13（600）；h1 下边框；标题段距 上1.1em 下0.5em |
| 列表 | padding-left 22；li 上下 0.15em |
| 引用 blockquote | 左边框 3 + 圆角右 6；淡背景 |
| 行内 code | padding 0 4；radius 4；font 12；浅底+1px hairline |
| 代码块 pre | padding 10 12；radius 8；font 12；行高1.55；横向滚动 |
| 表格 | th/td padding 上4 左右9；1px hairline；font 12.5 |
| 流式光标 .is-typing::after | 宽2 高13；蓝；1s step-end 闪烁 |

## 5. 过程链（chain.css）
### 5.1 折叠头 / 容器
| 项 | 值 |
|---|---|
| .chain | font 11.5；margin 上2 下6 |
| .chain-summary | inline-flex gap6；padding 上2 左右6；radius 6；font 11.5；hover 透明底 |
| .chain-summary-ico / -chev | codicon 12 |
| .chain-detail | 上 margin 3；子项 gap 4 |

### 5.2 思考/工具 Disclosure 行
| 项 | 值 |
|---|---|
| .chain-disclosure | 上下 margin 3 |
| .chain-row-head | flex 行 gap 6；padding 上2 左右3；radius 5；width 100%；font 11.5；nowrap+keep-all；hover 透明底 |
| .chain-chev / .chain-kind-ico / .chain-tool-ico | 12px，flex none |
| .chain-row-title / .chain-tool-title | 600；nowrap；超过省略 |
| .chain-tool-err | font 11；红；max-width 40~45%；nowrap 省略 |
| .chain-tool-status | 12；margin-left auto；ok 无角标/绿仅色可留 |
| 正文预览 .chain-row-preview | font 11；line-height 1.5；flex 1 1 auto、min-width 0；**收起态单行** `nowrap+ellipsis`——宽不足才省略、无固定字数上限；全文靠展开 |
| 展开体 .chain-disclosure-body | 上1 下2 margin |

### 5.3 思考正文框 / 工具参数
| 项 | 值 |
|---|---|
| .chain-reason | padding 上2 左右8；radius 5；font 11.5；行高1.55；淡底 |
| .chain-reason.is-live | max-height 160；纵向内滚（自动贴底） |
| .chain-reason.is-open | max-height none |
| .chain-tool-out（真实输出） | margin 上3 左右2；padding 上6 左右8；radius 6；font 11（等宽）；行高1.55；浅底+hairline |
| .chain-tool-json（原始参数 pretty） | 同上；white-space pre（横向滚动） |
| .chain-arg-k | 次级色；右 margin 8 |
| .chain-arg-v | foreground；pre-wrap |

### 5.4 Terminal 卡（官方 TerminalBlock.module.css 复刻，bash/pwsh/shell）
| 项 | 值 |
|---|---|
| .term | radius 12；gutter 左 pad 30；margin `4px 0 4px 4px`；0.5px hairline 边框；bg=textCodeBlock；font 12(等宽)；overflow hidden |
| .term-head（命令横幅） | flex；gap 12；margin-left -30(拉平)、padding `9px 14px 9px 30px`；max-height 150 内滚；定稿下 0.5px 底分隔 |
| .term-prompt-line | 每命令一行；line-height 22；gap 8 |
| .term-dot（状态点） | 绝对定位 left -22、垂直居中；尺寸 10×10 圆；ongoing 蓝 / done 绿 / error 红，各带 0.18/0.15(alpha) 同色 halo |
| .term-cwd | 提示标签（`~`/末段/`$`）；tertiary 色；flex none |
| .term-cmd | **white-space pre**（保留对齐）；超长 ellipsis；primary 色 |
| .term-status（退出码/信号 Pill） | sticky top；高 22；error 红 |
| .term-copy 按钮 | sticky top；font 12；tertiary 色；无 border；复制原始输出(剥 marker) |
| .term-output | padding `12px 14px 12px 0`；max-height 224；横向+纵向内滚 |
| .term-line | min-height 22；white-space pre |
| .term-expand（… 其余 n 行） | 整行按钮；tertiary 色；hover 变 primary；text-align left |
| .term-empty（无输出） | padding `12px 14px 12px 0`；tertiary 色 |

### 5.5 上下文注入/召回行（官方 ContextInjectionRow.module.css / ContextBody.module.css 复刻）
| 项 | 值 |
|---|---|
| .ctx-head | flex 行；gap 6；padding 上2 左右3；radius 5；nowrap+keep-all；hover 透明底 |
| .ctx-chev / .ctx-ico | codicon 12；flex none；次级色 |
| .ctx-title | 600；nowrap 省略 |
| .ctx-sep（分隔圆点） | 2×2；margin 0 8；radius 1 |
| .ctx-source（来源标签） | flex none；单行省略；tertiary 色；font 11.5 |
| .ctx-summary（notice 单行说明） | flex 1 1 auto；单行省略；tertiary 色；font 11.5 |
| .ctx-body（展开体） | width `calc(100% - 22px)`；max-height 141；margin 上4 左22；padding `10px 16px 12px 12px`；radius 8；bg=textCodeBlock；font 11（等宽） |
| body 内文本 .ctx-text | `white-space: pre-wrap; overflow-wrap: anywhere` |
| 源字段 .ctx-fields | 顶部 0.5px hairline 分隔；gap 2；fieldKey 左 96 固定、caption 色；fieldValue 自适应、tertiary 色 |
| 各 form 列表 | files(flex wrap gap 4/12)、entries/recalls(纵向 gap 4/2)、sections(纵向 gap 8)；entry-name/section-name 次级、description/counts caption 色 |

### 5.6 Web 卡（官方 WebBlock.module.css 复刻；web_fetch/web_search）
| 项 | 值 |
|---|---|
| .web | margin `4px 0 4px 4px`；padding `12px 14px`；radius 12；bg=textCodeBlock；font 13；color foreground |
| .web-fetch | 纵向 flex；gap 6；布局=**URL 链接行(globe 图标) → `.web-status`(下一行)** |
| .web-status | font 13/600；次级色；位于 URL 链接**下一行**（用户指定：状态在链接下；官方 URL 上、状态下） |
| .web-link | 13px/500；line-height 19；link 色；等宽；break-all；hover 虚线下划线；链接前 **globe 浏览器图标** |
| .web-link-ico | 13px；vertical-align -2px；右 margin 5 |
| .web-truncated | 12px；tertiary 色；上 margin 8（fetch 卡内 0） |
| .web-answer | 下 margin 8；foreground；正文 markdown |
| .web-empty | 12px；次级色 |
| .web-sources | padding-left 2.5em；纵向 flex；gap 10；max-height 320；内滚 |
| .web-source-link | 14px/500；line-height 20；link 色；break-word；hover 虚线下划线 |
| .web-snippet | 13px/19；次级色；上 margin 2 |
| .web-published | 12px；tertiary 色；上 margin 2 |

### 5.7 提问卡（官方 AskQuestionCard.module.css 复刻；ask_user_question）
| 项 | 值 |
|---|---|
| .ask-card | flex 纵向；gap 16；max-height 360；margin `4px 0 4px 4px`；padding `16px 20px`；内滚；0.5px hairline 边框；radius 12；bg=editorWidget |
| .ask-item | 纵向；gap 2 |
| .ask-question / .ask-answer / .ask-verdict / .ask-q-unanswered | margin 0；13px；line-height 1.6；pre-wrap；问题/未答 tertiary、answer/verdict foreground |
| .ask-answer-line | block |
| .ask-skipped | tertiary 色 |
| .ask-question-list | padding-left 20；gap 8 |
| .ask-card | 提问记录：问题→答案列表 / 结论+问题列表（只读） |
| .q-dialog（waterfall 交互弹窗，composer 上方） | margin `0 14px 4px`；padding `12px 14px`；radius 8；bg=editorWidget；flex 列；gap 10 |
| .q-dialog-opt | padding `4px 10px`；radius 6；1px hairline；hover 透明底；selected 主题背景+focusBorder |
| .q-dialog-input | padding `6px 10px`；radius 6；1px hairline；bg=inputBackground |
| .q-dialog-submit / .cancel / .close | `回答`=button 底/前景（选全可点）；`取消`=透明+tertiary；`关闭`(✕)=margin-left auto |

## 6. 状态 / 过渡（state.css）
| 项 | 值 |
|---|---|
| .composer.is-busy | opacity 0.65；输入/发送 pointer-events none |
| .composer-busy | font 11；padding 上2 左右2 下5；gap 6；codicon 12 |
| busy 文案 | 正在加载会话… / 正在切换… |

## 7. 输入区 composer（chat.css）
| 项 | 值 |
|---|---|
| #composer | padding 上8 左右12 下4；editor 底 |
| #inputbox | radius 10；1px hairline；focus-within 主题 focusBorder |
| textarea #input | padding 上10 左右14 下4；font 13；min-height 68 / max-height 150 |
| #inputbar | padding 上2 左右10 下4 左2；gap 4 |
| 发送钮 #send | 24×24 圆；radius 50%；主题按钮底；stop 态 error 底 |
| 图标钮(权限/模型/模式) | 22×22；radius 4~6 |
| chips(plan/goal) | font 11；radius 10；gap 6 |

## 8. 弹层/通用（chat.css）
| 项 | 值 |
|---|---|
| .popup | radius 8；padding 6；list gap 1；浮层底+阴影 |
| #triggerPopup | width min(340,92vw) |
| .spicker 搜索框 | radius 5；font 12 |
| .modal-card | max-width 300；radius 10；padding 14；gap 12 |
| 按钮 .btn | radius 6；padding 上5 左右14；font 12 |

## 9. 动效
| 项 | 值 |
|---|---|
| 折叠头 chevron | transition transform 0.12s ease |
| msg-meta 显隐 | opacity 0.15s |
| 流式光标 | 1s step-end 无限 |
| LiveVerb 动词轮换 | 间隔 1.6s |
| 状态转圈 | codicon-modifier-spin（1.5s steps 30，内建） |

> 附：颜色类语义（fallback）charts-blue #4daafc / charts-green #89d185 / error #f14c4c / description #999~888。
