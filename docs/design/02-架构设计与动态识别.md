# 02 · 架构设计与动态识别

> 阶段：设计 · 维护者：插件维护者

## 分层
单向依赖 UI → 服务 → API，UI 可换、协议可换。

| 层 | 载体 | 职责 | 可替换 |
|---|---|---|---|
| 装配 | extension.ts | 命令/面板/桥接 | 只加命令 |
| UI | webview/chat、`dshPanel.ts`（上游网页面板） | 聊天、内嵌 dsh 网页 | 可整体换 |
| 服务 | api/dshService.ts | dsh 进程/会话/对话编排 | 稳定接口 |
| 事件 | dsh/events.ts | $events 监听流与应答回传 | 只依赖 dsh 门面 |
| API | dsh/{auth,rpc,mux,session,stream,legacy,agentPresets}（api.ts/index.ts 门面；agentPresets 于 v0.1.4 · dsh 0.1.2-rc.1 新增） | dsh 协议层 | 换后端只改 dsh/ |

## 整体架构

两个进程内各有一份独立产物：webview 侧 `dist/chat/chat.js`（浏览器上下文，内嵌 htm/preact），扩展进程侧 `dist/extension.js`（Node）。两侧只通过 `postMessage` 通信，消息形状由 `webview/chat/core/protocol.ts` 单方面约定（宿主侧无同名类型，见 §已知缺口）。

```
VS Code 窗口
┌───────────────────────────────────────────────────────────────┐
│ webview:chat（浏览器上下文，产物 dist/chat/chat.js）           │
│                                                               │
│  chat.ts 入口                                                 │
│    acquireVsCodeApi 得 ChatHost                               │
│    之后 new createChatStore(host)                             │
│                                                               │
│  core/store/chat.ts 装配层（只接线，不含行为）                 │
│    messages  行模型：列表 / 正文流式 / 过程归约 / 审批         │
│    composer  输入区：文本 / 图片 / 附件 / @引用贴片            │
│    selectors 权限 · 模型 · 模式 与弹窗                         │
│    catalogs  「/」命令目录 与「@」候选目录                     │
│    question  提问弹窗    status  会话投影状态                  │
│    history   历史恢复    outbox  用户发起的发送动作            │
│    reducer   宿主消息归约器（唯一分发点）                      │
│    各切片互不 import；跨切片依赖由装配层注入                   │
│                                                               │
│  components/ 只读 store 信号渲染，不回写                       │
└───────────────────────────────────────────────────────────────┘
                              │
              postMessage（契约：core/protocol.ts）
                              │
┌───────────────────────────────────────────────────────────────┐
│ extension（扩展进程，产物 dist/extension.js）                  │
│                                                               │
│  extension.ts 装配：命令 / 聊天 UI / 面板 / webview 桥接       │
│    dshPanel.ts        内嵌网页面板                             │
│    api/dshService.ts  进程 · 会话 · 对话编排                   │
│    dsh/               协议层，门面 api.ts + index.ts           │
│      auth        端点与 cookie 鉴权                            │
│      rpc         信封 · commands · probe · 端口                │
│      mux         Mini WebSocket · /api/remote.mux              │
│      session     会话事件模型 · follow · model · workspace     │
│      stream      waitTurn · 流式对话                           │
│      events      $events 监听与应答                            │
│      webProxy    iframe 本地认证代理                           │
│      agentPresets 会话模式 roster                              │
└───────────────────────────────────────────────────────────────┘
                              │
                  HTTP JSON-RPC ＋ remote.mux WS
                              │
                       dsh 进程（本机）
```

一轮对话的往返（两条路径，产物都是同一份过程链）：

```
用户操作（发送 / 建议 / 重新生成）
  outbox 组装 prompt 与展示文本
    composer 的输入区状态 → 清空
    messages 追加用户行与回答行
  经 ChatHost 发 chatSend
        │
        ▼
  extension 收帧 → dshService → dsh/ 协议层 → dsh
        │
        ▼
  dsh 事件回流
    stream 侧实时（follow）：chatActivity / chatReasoning / chatContext / chatChunk / chatDone
    session 侧历史（快照）：chatHistory 一次性重放
        │
        ▼
  webview 收帧 → reducer 分发
    过程项（思考 / 工具 / 上下文注入）落入当前回答行的 chain
    正文落入回答行 text，链随之定稿可收起
```

## 服务层接口（聊天 UI 依赖）
| 类别 | 方法 | 说明 |
|---|---|---|
| 通用 | call(method, payload) | 任意 dsh API |
| 进程 | ensureRunning / dispose | 启动/复用/回收；ensureRunning 单飞，并发入口共享同一次启动 |
| 会话 | getSession / newSession / ensureCurrentWorkspace / ensureWorkspaceForFolder | 会话与当前工作区 |
| 工作区 | listWorkspaces / createWorkspace / listWorkspaceSessions / restoreSession | 枚举/新建/恢复 |
| 对话 | ask / askStreaming / approvalResponse / answerQuestion / cancelQuestion | 发送/流式/审批/提问/停止 |
| 模型/权限 | listModels / selectModel / setPermissionPreset | 模型/权限 |
| 投影 | getProjections | 上游投影 |

上游网页面板 UI 由 `src/dshPanel.ts`（openPanel/openInBrowser/openInEditor/reloadPanels/viewMode）提供，通过宿主 `ensureRunning()` 使用 DshService；关闭面板只回收本地代理，不回收 dsh 进程。

## 技术选型
VS Code 扩展（vscode>=1.110，node ^22.19\|\|>=24）+ 原生 webview(无框架) + esbuild(产物 dist/extension.js) + scripts/build-chat.mjs(产物 dist/chat) + 本机 HTTP JSON-RPC + `/api/remote.mux` WS + 会话归 dsh + 仅 `globalState['dsh.usage']` 持久化。无数据库。

## 目录与依赖方向
| 模块 | 职责 | 依赖 |
|---|---|---|
| extension.ts | 装配：命令、聊天 UI、DshPanel | DshService、dsh 门面 |
| dshPanel.ts | 上游网页面板 UI | DshService.ensureRunning、dsh/webProxy |
| api/dshService.ts | dsh 进程/会话/对话编排 | dsh 门面 |
| dsh/auth.ts | 端点 + cookie 鉴权 | node:http |
| dsh/rpc.ts | RPC 信封/commands/probe/端口 | dsh/auth |
| dsh/mux.ts | Mini WebSocket + remote.mux | dsh/auth |
| dsh/session.ts | 会话事件模型/follow/session/model/workspace | dsh/rpc、dsh/mux |
| dsh/stream.ts | waitTurn / ask | dsh/session、dsh/mux |
| dsh/events.ts | $events 监听与应答 | dsh/rpc、dsh/mux |
| dsh/webProxy.ts | iframe 本地认证代理 | dsh/auth、dsh/rpc |
| dsh/agentPresets.ts | 会话模式 roster 读取/切换（agentPresets/list、select）【v0.1.4 · dsh 0.1.2-rc.1 新增】 | dsh/rpc |

依赖是单向的：auth → rpc/mux → session → stream；events/webProxy/agentPresets 位于其上层，无循环。

webview 聊天页（`webview/chat/`，与上表互不 import，只经 postMessage 通信）【v0.1.7 · dsh 0.1.2-rc.1 拆分】：

| 模块 | 职责 | 依赖 |
|---|---|---|
| chat.ts（入口） | 取 ChatHost、建 store、挂消息与标题栏处理器 | core/store/chat、core/host |
| core/protocol.ts | 页面与宿主 postMessage 的消息类型单一来源 | 无（类型叶） |
| core/store/chat.ts | 装配层：建各切片、注入跨切片依赖、聚合 `reset`、显式装配 `ChatStore` | 下列全部切片 |
| core/store/types.ts | 行模型（`ChatRow`/`DshTurnProcessItem`）与 `ChatStore` 接口 | core/protocol（仅类型） |
| core/store/messages.ts | 消息行模型：列表、正文流式、过程归约、通知行、审批行 | core/types、core/format |
| core/store/composer.ts | 输入区：文本 / 图片 / 附件 / @引用贴片 + 宿主直通动作 | core/types、core/host |
| core/store/selectors.ts | 权限 · 模型 · 模式 的选项状态与弹窗 | core/types、core/host |
| core/store/catalogs.ts | 「/」命令目录与「@」候选的拉取与到达 | core/types、core/host |
| core/store/question.ts | 提问弹窗（waterfall） | core/types、core/host |
| core/store/status.ts | 会话投影状态：过渡态 / 统计行 / plan / goal / 系统提示词 | core/types、core/format |
| core/store/history.ts | 历史恢复：快照重放为对话行 | core/types、core/format |
| core/store/outbox.ts | 用户发起的发送动作：发送 / 建议 / 重新生成 / 挂起「/」行裁决 | core/types、core/host |
| core/store/reducer.ts | 宿主消息归约器：消息类型到切片方法的唯一分发点 | core/types（仅类型引用兄弟切片） |
| components/ | 只读 store 信号渲染 | core/store/chat（类型）、core/* |

切片之间无 import 边（`reducer` 只用 `import type` 引用兄弟切片类型），跨切片依赖一律由装配层以实例注入；`core/trigger`、`core/format`、`core/protocol` 是共享的叶模块。

## 动态识别
- 连接模型：`DshEndpoint{port, authUrl}`，默认 `{port:3080}`；`endpointBaseUrl()` 明文、`endpointAuthUrl()` 优先带 token；所有传输从 `currentEndpoint` 取端口。
- 端口发现（ensureRunning 阶段）：① probeDsh(配置/3080)命中复用；② Node 检查；③ 启动器四档回退 cliPath → repoPath(`pnpm dsh web`) → PATH dsh → npx；固定参数 `web --no-open --port 0`（可经 `dsh.webArgs` 追加 `--host`/`--trusted-host` 等）；④ 解析 stdout `dsh web: http://127.0.0.1:PORT?token=…`(正则 `/dsh web: (https?:\/\/[^\s]+)/`)；⑤ 验信封 → ready。90s 未就绪报 stderr 末行并回收；超时回退 3080。
  复用注意：rc.1 手动实例没有 `dsh web` URL 时插件拿不到 authUrl、无法自动登录，因此一般不复用；建议 `dsh.port=0` 让插件自启。
- 握手探测 probeDsh：只读 `session/list`。判定：server-response+ok=可达；server-response+ok=false(DshRpcError)=可达(版本差异)；401/403=需鉴权；其它/非 JSON=端口非 DSH；连接失败=未运行。
- 事件投影：`DSH_EVENT_TYPES` 常量 + `projectHistoryEvent()` 判别联合；dsh 改事件名只改这两处。
- 鉴权：dsh v0.1.2-rc.1 起 `/api`、WS、网页 `/` 都要求 `dsh-auth-*` cookie
  （`?token=` 换 HttpOnly + SameSite=Strict）。Webview iframe 无法保存该 cookie，
  内嵌面板走 `dsh/webProxy.ts` 本地认证代理（扩展进程持有 cookie，转发 HTTP/WS 到真实端口）；
  外部浏览器直接用 `endpointAuthUrl()`。
- 面板代理：打开面板前先 `ensureEndpointAuth()`，再 `startDshWebProxy()`；
  只监听 127.0.0.1 随机端口，iframe 加载代理同源地址；面板全部关闭时只回收代理端口，
  dsh 进程保留到扩展停用（dispose），避免关闭网页面板后聊天再次启动服务。
  代理对上游 401 自动重新登录并重试（无实体请求），仍失败时给可读错误页而不铺上游裸 401。

## 降级
| 场景 | 路径 |
|---|---|
  | RPC 可用 | 自绘聊天 / 上游页本地代理 |
| RPC 不可用 / 鉴权失败 | 报错并引导在浏览器打开 `endpointAuthUrl()` |

## 可插拔
| 层 | 可插拔点 | 机制 |
|---|---|---|
| UI | 聊天整体可换 | 只依赖 DshService |
| 能力 | mux/流式/审批/提问 | `DshCapabilities{mux,streaming}` 门控 |
| 协议 | 后端可换 | 只改 dsh/ 下模块 |

复杂 UI 单元按 `ChatFeature{id,mount,unmount,enabled}` 注册；能力门控读 capability、配置门控读 dsh.port/cliPath/repoPath、危险功能 danger-full-access 需确认。新能力流程：dsh/ 加方法 → dshService 封装(读 capability) → UI 加 feature(读 enabled) → 可选加配置。

## 关键设计点
共享会话（右键/对话/网页同一线）；当前工作区 = workspace/follow updatedAt 最新（不另存）；新建会话必须归属工作区（显式选 / 当前 / 自动默认），三者皆无抛 `DshNoWorkspaceError`，不再回退 `createSession({})` 的「未分组」【v0.1.7(dev) · dsh 0.1.2-rc.1】；视图重建握手（webview 发 ready 后扩展再推 chatInfo）。

## 已知缺口 / 后续
待办/增强统一在待办清单维护，不在此文档列出。

## 更新记录

### 0.1.0（2026-08-30）
- 新增：
  - 初始版本

### 0.1.2（2026-09-03）
- 新增：
  - 架构与动态识别文档
- 修改：
  - 鉴权说明改为 rc1 cookie 机制；内嵌面板走 `dsh/webProxy.ts` 本地认证代理
  - 降级表更新：RPC 可用走自绘聊天或上游页本地代理；不可用时引导浏览器打开
  - 分层表 UI 行补 `dsh/webProxy`，内嵌 dsh 网页不再直连 `?token=` URL
  - `dsh/webProxy` 增加 401 自动重登录重试与可读错误页兜底
  - 端口发现补 rc.1 复用注意：手动实例无 authUrl 时建议 `dsh.port=0` 自启
  - 启动参数支持 `dsh.webArgs` 追加额外 CLI 参数（不影响固定 `--no-open --port 0`）
  - 分层表新增 dsh/events（$events 监听流），审批/提问改为聊天内应答
  - dsh 代码收入 `src/dsh/` 并加门面；上游网页面板抽到 `dshPanel.ts`（UI 层），DshService 不再混面板
  - 上游网页面板全部关闭不再回收 dsh，仅回收本地代理；进程随扩展停用回收
  - 服务层接口表移除面板行，上游网页归 `DshPanel`
  - ensureRunning 改为单飞等待
  - 新增“目录与依赖方向”章节

### 0.1.3（2026-09-05）
- 修改：
  - 会话恢复改为等待聊天视图 ready 后广播到侧栏/编辑器全部聊天实例，避免投递目标丢失
  - 新会话不再强制改名，会话标题交由 dsh 自动生成

### 0.1.4（2026-09-05）
- 修改：
  - dsh 协议层新增 `src/dsh/agentPresets.ts`（agentPresets/list + select）；服务层新增 list/switch 方法

### 0.1.5（2026-09-06）
- 修改：
  - 标题栏改双实现（原生 view/title / 页内自绘 `#titlebar`），扩展侧与 webview 侧各经装配层接线（`src/titlebar.ts` ↔ `webview/chat/titlebar.ts`，自绘实现 `titlebarSelfDrawn.ts`），`TITLEBAR_MODE` 唯一开关、默认 `nativeTitle`
  - 侧边栏视图按 `TITLEBAR_MODE` 直选原生/自绘；moveToEditor 编辑器面板恒注入「无标题栏」标记

### next（2026-09-10）
- 修改：
  - 会话归属硬约束：`newSession()` 不再以无参 `createSession({})` 回退（杜绝「未分组」）；取不到工作区抛 `DshNoWorkspaceError`。`listModels()` 改为无会话/无工作区时不强建会话，只回目录
  - 交互层统一拦截 `NO_WORKSPACE`：＋新会话 / 发消息 / 切模型/模式/权限 / 斜杠命令，无工作区时提示选工作区并弹面板，不建会话
  - 删除死代码 `stream.ts` 的 `ask()`（内部裸 `createSession()`，无人调用）
  - 新增「整体架构」小节：补整体分层框图（webview 与扩展进程两侧、postMessage 契约位置、dsh 协议层）与一轮对话的往返流程框图
  - 「目录与依赖方向」补 webview 聊天页模块表（此前只覆盖 `src/`，webview 内部结构无记录）：入口、协议、装配层与十个 store 切片、components 的职责与依赖
  - 记录 chat store 两阶段拆分：`core/store/chat.ts` 由 1006 行降为 111 行纯装配层，行为分散到 `messages`/`composer`/`catalogs`/`selectors`/`question`/`status`/`history`/`outbox`/`reducer` 与类型模块 `types.ts`；切片间无 import 边，跨切片依赖由装配层注入；`ChatStore` 接口不变
